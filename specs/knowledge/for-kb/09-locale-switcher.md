# 语言切换：顶栏灰色文字链

公网与管理台的语言切换放在 **右上角**，不要放在页脚。

## 位置

- 管理壳 / 接入指南顶栏：`.header-end` 最右侧（在 Hello 或「返回」之后）。
- 首页 / 登录等无顶栏页：`.shell-locale` 绝对定位到视口右上。

## 视觉（性冷淡）

- 灰色文字链，不是黑白填墨分段按钮。
- 文案：`中文` / `EN`。
- 未选：偏灰；当前：近黑 + 1px 底线。
- 字重约 500，略加字距与项距；零圆角、无地球图标、无圆角胶囊。

曾试过「墨盒反相」分段控件，偏重，已弃用。
